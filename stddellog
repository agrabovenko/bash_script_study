#!/bin/bash
set -euo pipefail

LOG_FILE="deleted.log"

if [ $# -ne 1 ]; then
  echo "Usage: $0 <directory>"
  exit 1
fi

DIR="$1"

if [ ! -d "$DIR" ]; then
  echo "Error: $DIR is not a directory"
  exit 2
fi

find "$DIR" -type f -name "*.log" -mtime +30 -print -delete >> "$LOG_FILE"


